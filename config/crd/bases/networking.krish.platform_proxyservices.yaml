---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: proxyservices.networking.krish.platform
spec:
  group: networking.krish.platform
  names:
    kind: ProxyService
    listKind: ProxyServiceList
    plural: proxyservices
    singular: proxyservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProxyService is the Schema for the proxyservices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ProxyService
            properties:
              listenPort:
                format: int32
                maximum: 65535
                minimum: 1024
                type: integer
              rateLimit:
                format: int32
                minimum: 0
                type: integer
              replicas:
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              upstreams:
                items:
                  type: string
                type: array
            required:
            - listenPort
            - rateLimit
            - replicas
            - upstreams
            type: object
          status:
            description: status defines the observed state of ProxyService
            properties:
              activeUpstreams:
                items:
                  type: string
                type: array
              availableReplicas:
                format: int32
                type: integer
            required:
            - activeUpstreams
            - availableReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
